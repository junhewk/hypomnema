services:
  hypomnema:
    build: .
    ports:
      - "8073:8073"
    volumes:
      - ./data:/app/data
    environment:
      - HYPOMNEMA_EMBEDDING_PROVIDER=local
      - HYPOMNEMA_PASSPHRASE=${HYPOMNEMA_PASSPHRASE:-}
    restart: unless-stopped
